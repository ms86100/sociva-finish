bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]/**
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount] * Maps notification_queue items → WhatsApp Cloud API sends.
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount] * Called from process-notification-queue as an additional channel (never replaces push/in-app).
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount] *
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount] * Until sociva_* templates are APPROVED, free-form fallback only delivers inside Meta's
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount] * ~24h customer service window (after the user messages the business). In-app opt-in CTA
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount] * (wa.me Hi) is user-initiated and does not require a "register me" template.
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount] */
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]import {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  logWhatsAppMessage,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  normalizeWhatsAppPhone,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  sendWhatsAppTemplateOrText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  type WhatsAppSendResult,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]} from "./whatsapp.ts";
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]const WA_ELIGIBLE_TYPES = new Set([
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "order",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "order_status",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "order_update",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "booking_reminder_1_hour",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "booking_reminder_30_min",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "booking_reminder_10_min",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "delivery",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "delivery_en_route",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "delivery_stalled",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "settlement",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "review",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "review_received",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "review_nudge",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "low_rating_alert",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "seller_daily_summary",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "seller_approved",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "seller_rejected",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "seller_suspended",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "license_approved",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "license_rejected",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "product_approved",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "product_rejected",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]]);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]/** Society/chat noise — never WhatsApp. Marketing is gated separately. */
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]const SKIP_TYPES = new Set([
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "chat",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "chat_message",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "visitor",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "parcel",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "dispute",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "snag",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "milestone",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "worker_job",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  "moderation",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]]);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]const MARKETING_TYPES = new Set(["promotion", "campaign"]);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]/** Normalize mixed enqueue payload shapes (orderId vs order_id, status vs new_status). */
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]export function normalizeWaPayload(payload: Record<string, unknown> | null | undefined): Record<string, unknown> {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const p = { ...(payload || {}) };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!p.status && p.new_status) p.status = p.new_status;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!p.orderId && p.order_id) p.orderId = p.order_id;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!p.order_id && p.orderId) p.order_id = p.orderId;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!p.sellerName && p.seller_name) p.sellerName = p.seller_name;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!p.providerName && (p.sellerName || p.provider_name)) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    p.providerName = p.sellerName || p.provider_name;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  return p;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]function toWaPhone(raw: string | null | undefined): string | null {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!raw) return null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const digits = String(raw).replace(/\D/g, "");
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (digits.length === 10) return normalizeWhatsAppPhone(`91${digits}`);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  return normalizeWhatsAppPhone(digits);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]function shortOrderRef(payload: Record<string, unknown>, title: string): string {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const id = String(payload.orderId || payload.order_id || "").slice(0, 8);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (id) return id.toUpperCase();
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  return title.slice(0, 24) || "order";
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]function statusHeadline(status: string): string {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const map: Record<string, string> = {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    assigned: "Provider assigned",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    provider_changed: "Provider updated",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    on_the_way: "On the way",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    arrived: "Provider arrived",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    at_gate: "At your gate",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    in_progress: "Service started",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    picked_up: "Order picked up",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    ready: "Order ready",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    delivered: "Delivered",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    completed: "Completed",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    confirmed: "Booking confirmed",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    scheduled: "Booking scheduled",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    rescheduled: "Rescheduled",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    cancelled: "Cancelled",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    no_show: "No-show",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    quoted: "Quote received",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    accepted: "Accepted",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    preparing: "Preparing",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    settlement_eligible: "Settlement eligible",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    settlement_paid: "Settlement paid",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    released: "Payment released",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  return map[status] || status.replace(/_/g, " ") || "Update";
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]export function shouldSendWhatsApp(opts: {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  type: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  whatsappPref?: boolean | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  whatsappOptedInAt?: string | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  promotionsPref?: boolean | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  payload?: Record<string, unknown> | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}): boolean {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Hard Meta opt-in: require explicit whatsapp_opted_in_at (grandfathered in migration).
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!opts.whatsappOptedInAt) return false;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (opts.whatsappPref === false) return false;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (opts.payload?.skip_whatsapp === true) return false;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (SKIP_TYPES.has(opts.type)) return false;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Marketing: require promotions + whatsapp prefs AND explicit enqueue flag
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (MARKETING_TYPES.has(opts.type) || opts.payload?.allow_whatsapp_marketing === true) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return (
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      opts.promotionsPref === true &&
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      opts.whatsappPref !== false &&
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      !!opts.whatsappOptedInAt &&
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      opts.payload?.allow_whatsapp_marketing === true
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    );
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (opts.payload?.wa_template) return true;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (WA_ELIGIBLE_TYPES.has(opts.type)) return true;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (opts.type.startsWith("booking_") || opts.type.startsWith("delivery_")) return true;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (opts.type.startsWith("seller_") || opts.type.startsWith("license_") || opts.type.startsWith("product_")) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return true;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (opts.type.startsWith("review") || opts.type.startsWith("settlement")) return true;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  return false;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]function resolveTemplate(opts: {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  type: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  title: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  body: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  payload: Record<string, unknown>;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  userName: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}): { templateName: string; bodyParams: string[]; fallbackText: string } {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const payload = normalizeWaPayload(opts.payload);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const status = String(payload.status || "").toLowerCase();
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const targetRole = String(payload.target_role || "");
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const name = opts.userName || "there";
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const orderRef = shortOrderRef(payload, opts.title);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const itemName = String(payload.itemName || "Item");
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const actualAmount = String(payload.actualAmount || "0");
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const sellerSocietyId = String(payload.sellerSocietyId || "");
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const buyerFlatNo = String(payload.buyerFlatNo || "");
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const buyerBlock = String(payload.buyerBlock || "");
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const buyerPhase = String(payload.buyerPhase || "");
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const fallbackText = `${opts.title}\n${opts.body}\n— Sociva`;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (payload.wa_template) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    const tpl = String(payload.wa_template);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    if (tpl === "sociva_booking_confirmed") {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        templateName: tpl,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        bodyParams: [
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          name,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.providerName || payload.sellerName || "your provider"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          orderRef,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.serviceDate || payload.date || "—"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.serviceTime || payload.time || "—"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          itemName,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          actualAmount,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        ],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    if (tpl === "sociva_booking_cancelled") {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        templateName: tpl,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        bodyParams: [
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          name,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          orderRef,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.providerName || "your provider"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.reason || opts.body || "Cancelled").slice(0, 200),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          itemName,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          actualAmount,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        ],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    if (tpl === "sociva_booking_reminder") {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        templateName: tpl,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        bodyParams: [
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          name,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.providerName || "your provider"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.serviceDate || "—"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.serviceTime || "—"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          orderRef,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          itemName,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          actualAmount,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        ],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    if (tpl === "sociva_new_order_seller") {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        templateName: tpl,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        bodyParams: [
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          String(payload.status || "order"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          `${orderRef} — ${opts.body}`.slice(0, 200),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          itemName,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]          actualAmount,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        ],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    if (
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      tpl === "sociva_refund_update" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      tpl === "sociva_payment_update" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      tpl === "sociva_order_update" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      tpl === "sociva_store_status"
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    ) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        templateName: tpl,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        bodyParams: [name, orderRef || opts.type, (opts.title || statusHeadline(status) || "Update").slice(0, 120), itemName, actualAmount],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Marketing (opt-in already gated in shouldSendWhatsApp)
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (MARKETING_TYPES.has(opts.type) || payload.allow_whatsapp_marketing === true) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_order_update",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [name, "offer", (opts.title || "Sociva offer").slice(0, 120), itemName, actualAmount],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Seller new order / enquiry
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    targetRole === "seller" &&
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    ["placed", "enquired", "requested", "auto_accepted"].includes(status)
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  ) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_new_order_seller",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [status || "order", `${orderRef} — ${opts.body}`.slice(0, 200), itemName, actualAmount],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Booking / order lifecycle for buyers
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (["accepted", "auto_accepted", "confirmed", "scheduled", "preparing"].includes(status)) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_booking_confirmed",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        name,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        String(payload.providerName || payload.sellerName || "your provider"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        orderRef,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        String(payload.serviceDate || payload.date || "soon"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        String(payload.serviceTime || payload.time || "—"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        itemName,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        actualAmount,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      ],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (status === "cancelled" || status === "no_show") {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_booking_cancelled",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        name,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        orderRef,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        String(payload.providerName || "your provider"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        String(payload.reason || opts.body || status).slice(0, 200),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        itemName,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        actualAmount,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      ],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (opts.type.startsWith("booking_reminder")) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_booking_reminder",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        name,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        String(payload.providerName || "your provider"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        String(payload.serviceDate || "today"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        String(payload.serviceTime || "soon"),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]        orderRef,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      ],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Provider / fulfillment lifecycle → utility order update
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    [
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "assigned",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "provider_changed",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "on_the_way",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "arrived",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "at_gate",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "in_progress",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "picked_up",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "ready",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "delivered",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "completed",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "rescheduled",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      "quoted",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    ].includes(status)
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  ) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_order_update",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [name, orderRef, statusHeadline(status).slice(0, 120), itemName, actualAmount],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    status.startsWith("refund") ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.title.toLowerCase().includes("refund") ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type.includes("refund")
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  ) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_refund_update",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [name, orderRef, (opts.title || "Refund update").slice(0, 120), itemName, actualAmount],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Settlements / earnings digest
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type === "settlement" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type === "seller_daily_summary" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    status.startsWith("settlement") ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    status === "released" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    status === "eligible" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    status === "settled"
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  ) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_payment_update",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [name, orderRef || "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120)],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Reviews
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type === "review_nudge" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type === "review" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type === "review_received" ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type === "low_rating_alert"
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  ) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_order_update",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [name, orderRef || "review", (opts.title || "Review update").slice(0, 120)],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type.startsWith("seller_") ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type.startsWith("license_") ||
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    opts.type.startsWith("product_")
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  ) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      templateName: "sociva_store_status",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120)],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  // Default utility order update
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  return {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    templateName: "sociva_order_update",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    bodyParams: [name, orderRef, (opts.title || statusHeadline(status) || "Update").slice(0, 120)],
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]export async function deliverWhatsAppForQueueItem(opts: {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  userId: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  phone: string | null | undefined;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  userName?: string | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  type: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  title: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  body: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  payload?: Record<string, unknown> | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  whatsappPref?: boolean | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  whatsappOptedInAt?: string | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  promotionsPref?: boolean | null;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  notificationId?: string;
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}): Promise<{ attempted: boolean; result?: WhatsAppSendResult & { usedTemplate?: boolean }; skipReason?: string }> {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const payload = normalizeWaPayload(opts.payload);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!shouldSendWhatsApp({
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    type: opts.type,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    whatsappPref: opts.whatsappPref,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    whatsappOptedInAt: opts.whatsappOptedInAt,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    promotionsPref: opts.promotionsPref,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    payload,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  })) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return { attempted: false, skipReason: "not_eligible_or_opted_out" };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const phone = toWaPhone(opts.phone);
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  if (!phone) {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    return { attempted: false, skipReason: "no_phone" };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  }
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const mapped = resolveTemplate({
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    type: opts.type,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    title: opts.title,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    body: opts.body,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    payload,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    userName: opts.userName || "there",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  });
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  const result = await sendWhatsAppTemplateOrText({
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    phoneNumber: phone,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    templateName: mapped.templateName,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    bodyParams: mapped.bodyParams,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    fallbackText: mapped.fallbackText,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  });
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  await logWhatsAppMessage({
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    direction: "outbound",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    phone,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    message: mapped.fallbackText.slice(0, 500),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    meta_message_id: result.metaMessageId || null,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    status: result.success ? "sent" : "failed",
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    error_code: result.success ? null : result.code,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    error_message: result.error || null,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    template_name: result.usedTemplate ? mapped.templateName : null,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    meta_payload: {
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      ...(typeof result.meta === "object" && result.meta ? result.meta : {}),
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      queue_notification_id: opts.notificationId || null,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      used_template: !!result.usedTemplate,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      user_id: opts.userId,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]      mapped_template: mapped.templateName,
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]    },
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  });
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]  return { attempted: true, result };
bodyParams: [name, orderRef || opts.type, (opts.title || "Account update").slice(0, 120), itemName, actualAmount]|| "review", (opts.title || "Review update").slice(0, 120), itemName, actualAmount]|| "payout", (opts.title || statusHeadline(status) || "Earnings update").slice(0, 120), itemName, actualAmount]}
