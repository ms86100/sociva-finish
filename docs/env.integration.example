# Integration-only env (never commit real keys into production project settings)
# Copy to `.env.integration.local` for local runs against the Supabase branch.

VITE_SUPABASE_URL=https://hfeuskcehglyqllvrxyu.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=REPLACE_WITH_ANON_KEY_FROM_INTEGRATION_BRANCH
